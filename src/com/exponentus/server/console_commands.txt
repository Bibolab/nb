quit, exit (q) - stop server with safety;
reset rules(rr) - reset loaded rules and caches;
reload vocabulary(rv) - reload vocabularies, templates and reset cashes;
show initializers(si) - show all available Initializers;
start initializer(stini) * - run a initializer;
run batch (rubat) * - run a batch file;
show server session pool (sssp) - show logged users;
show scheduled tasks (sst) - show scheduled tasks
show scheduler queue (ssq) - show scheduler queue 
import from h2 (ifh2) - import users list from H2 database;
info (i) - show some information of the environment;
modules info (mi) - show some information of the loaded modules;
database info (dbi) - show some information of the database;
help (h) - show this help;


